import ConversationContainer from '@/components/shared/conversation/ConversationContainer'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <ConversationContainer>Profile</ConversationContainer>
  )
}

export default page